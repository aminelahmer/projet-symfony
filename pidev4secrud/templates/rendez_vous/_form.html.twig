
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.dateRdv) }}
    {{ form_row(form.timeRdv) }}
    {{ form_row(form.idCoach) }}
    {{ form_row(form.idClient) }}
    {{ form_row(form.planning) }}
   
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

